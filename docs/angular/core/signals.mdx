---
description: Signals in Angular framework
id: angular-signals
title: Signals
slug: /angular-signals
sidebar_position: 1
tags: [Angular, Signals]
keywords:
  - angular
  - signals
last_update:
  date: 1/23/2024
  author: Admin
---

import CodeBlock from "@theme/CodeBlock";
import SignalsCounter from "!!raw-loader!@site/src/components/Angular/counter/signals-counter.component.tsx";
import SignalsCounterHTML from "!!raw-loader!@site/src/components/Angular/counter/signals-counter.component.html";

import UserProfileService from "!!raw-loader!@site/src/components/Angular/user-profile/profile-service.tsx";
import SignalUserProfile from "!!raw-loader!@site/src/components/Angular/user-profile/signals-user-profile.component.html";
import SignalsUserProfile from "!!raw-loader!@site/src/components/Angular/user-profile/user-profile.component.tsx";
import SignalsUserProfileHTML from "!!raw-loader!@site/src/components/Angular/user-profile/user-profile.component.html";

import CartService from "!!raw-loader!@site/src/components/Angular/cart-management/cart.service.ts";
import SignalCart from "!!raw-loader!@site/src/components/Angular/cart-management/cart.component.ts";
import SignalCartHTML from "!!raw-loader!@site/src/components/Angular/cart-management/cart.component.html";

:::info
The current document is being updated to Angular 17.
:::

### What are signals? :globe_with_meridians:

A signal is a wrapper around a value that notifies interested consumers when that value changes. Signals can contain any value, from simple primitives to complex data structures.

You read a signal's value by calling its getter function, which allows Angular to track where the signal is used.

Signals may be either `writable` or `read-only`.

### Writable signals

```js title="Create a signal"
  count: WritableSignal<number> = signal(0);
```

To change the value of a writable signal, use the `.set()`:

```js
count.set(3);
```

or use the `.update()` to add a new value from the previous one:

```js
count.update((value) => value + 1);
```

### Computed signals

```js
  count: WritableSignal<number> = signal(0);
  doubleCount: Signal<number> = computed(() => this.count() * 2); // read only
```

Computed signal are `read-only`. The `doubleCount` will not use `set()` or `update()`.

The `doubleCount` signal depends on the `count` signal. Whenever `count` updates, Angular knows that `doubleCount` needs to update as well.

### Effects

An `Effect` is an operation that runs whenever one or more signal values change.

`Effects` always run **at least once**.

`Effects` always execute **asynchronously**, during the change detection process.

```js
  @Component({...})
  export class EffectiveCounterComponent {
    readonly count = signal(0);
    constructor() {
      // Register a new effect.
      effect(() => {
        console.log(`The count is: ${this.count()})`);
      });
    }
  }
```

Alternatively, you can assign the effect to a field.

```js
  @Component({...})
  export class EffectiveCounterComponent {
    readonly count = signal(0);
    private loggingEffect = effect(() => {
      console.log(`The count is: ${this.count()})`);
    });
  }
```

To create an `Effect` outside of the constructor.

```js
  @Component({...})
  export class EffectiveCounterComponent {
    readonly count = signal(0);
    constructor(private injector: Injector) {}
    initializeLogging(): void {
      effect(() => {
        console.log(`The count is: ${this.count()})`);
      }, {injector: this.injector});
    }
  }
```

### Destroying effects

When you create an effect, **it is automatically destroyed when its enclosing context is destroyed**. This means that effects created within components are destroyed when the component is destroyed. The same goes for effects within directives, services, etc.

`Effects` return an `EffectRef` that you can use to destroy them manually, by calling the `.destroy()` method. You can combine this with the `manualCleanup` option to create an effect that lasts until it is manually destroyed. Be careful to actually clean up such effects when they're no longer required.

### Example 1: Counter :memo:

<CodeBlock language="tsx" title="signals-counter.component.ts">
  {SignalsCounter}
</CodeBlock>

<CodeBlock language="html" title="signals-counter.component.html">
  {SignalsCounterHTML}
</CodeBlock>

**Result**

![Result](/img/angular/signals_counter.png "Angular 17 - Signals Counter")

### Example 2: User Profile :memo:

Create a new **User Profile** component and then add it to **signals-user-profile.component.html**

```html title="signals-user-profile.component.html" {12-14}
<app-main-layout>
  <ng-container title> Signals </ng-container>
  <ng-container container>
    <mat-tab-group>
      <mat-tab label="Example">
        <h2>Counter: {{ count() }}</h2>
        <h2>DoubleCount: {{ doubleCount() }}</h2>
        <button mat-raised-button color="primary" (click)="updateCount()">
          Click
        </button>
      </mat-tab>
      <mat-tab label="User Profile">
        <app-user-profile />
      </mat-tab>
    </mat-tab-group>
  </ng-container>
</app-main>
```

In this example, we will use Signal in the service.

<CodeBlock language="tsx" title="profile-service.service.ts">
  {UserProfileService}
</CodeBlock>

<CodeBlock language="tsx" title="user-profile.component.ts">
  {SignalsUserProfile}
</CodeBlock>

<CodeBlock language="html" title="user-profile.component.html">
  {SignalsUserProfileHTML}
</CodeBlock>

**Result**

![Result](/img/angular/signals_user_profile.png "Angular 17 - Signals User Profile")

### Example 3: Cart Manager :memo:

In this example, we will learn about **Cart management** so that could add/delete the cart.

Create a new **Cart management** component and then add it to **signals-user-profile.component.html**

```html title="signals-user-profile.component.html" {15-17}
<app-main-layout>
  <ng-container title> Signals </ng-container>
  <ng-container container>
    <mat-tab-group>
      <mat-tab label="Example">
        <h2>Counter: {{ count() }}</h2>
        <h2>DoubleCount: {{ doubleCount() }}</h2>
        <button mat-raised-button color="primary" (click)="updateCount()">
          Click
        </button>
      </mat-tab>
      <mat-tab label="User Profile">
        <app-user-profile />
      </mat-tab>
      <mat-tab label="Cart Manager">
        <app-cart />
      </mat-tab>
    </mat-tab-group>
  </ng-container>
</app-main>
```

<CodeBlock language="tsx" title="cart.service.ts">
  {CartService}
</CodeBlock>

<CodeBlock language="tsx" title="cart.component.ts">
  {SignalCart}
</CodeBlock>

<CodeBlock language="html" title="cart.component.html">
  {SignalCartHTML}
</CodeBlock>

**Result**

![Result](/img/angular/signals_cart_management.png "Angular 17 - Signals Cart Management")

---

:eight_pointed_black_star: Link reference

:point_right: https://angular.dev/guide/signals

:point_right: https://hackernoon.com/a-guide-to-angular-signals-with-practical-use-cases-part-2
